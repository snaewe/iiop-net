This directory contains the .NET client for the Chatroom ejb running
on jboss 3.2.1, which uses java1.4!

Note: This client doesn't work with java1.3

Parameters (both optional):
1: the corbaloc or IOR of the Jboss name service
2: the callback port, the client is listing on